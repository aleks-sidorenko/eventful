name: examples-bank
version: "0.1.0"
maintainer: Alexander Sidorenko
synopsis: Example bank application for eventium
description: Example bank application for eventium
stability: experimental
license: MIT

dependencies:
  - base >= 4.9 && < 5
  - eventium-core
  - eventium-sqlite

  - aeson
  - aeson-pretty
  - bytestring
  - containers
  - contravariant
  - lens
  - monad-logger
  - mtl
  - optparse-applicative
  - persistent
  - persistent-sqlite
  - persistent-template
  - safe
  - stm
  - x-sum-type-boilerplate
  - template-haskell
  - text

library:
  source-dirs:
    - src
  ghc-options: -Wall

executables:
  bank-main:
    main: bank-main.hs
    ghc-options: -Wall
    source-dirs:
      - executables
    dependencies:
      - examples-bank

tests:
  spec:
    main: Spec.hs
    ghc-options: -Wall
    source-dirs:
      - tests
      - src
    dependencies:
      - hspec
      - HUnit
